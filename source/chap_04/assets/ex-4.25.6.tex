\documentclass[12pt, landscape]{article}
\usepackage{fouriernc}
\usepackage{bm}
\usepackage{amsmath}

\usepackage{tikz}
\usetikzlibrary{quantikz}

\pagestyle{empty}

\begin{document}

\begin{quantikz}[row sep={1cm,between origins}]
  \qw & \ctrl{2} & \midstick[3,brackets=none]{=}\qw &
  \qw & \qw & \ctrl{2}\gategroup[3,steps=1,style={dashed,
    rounded corners,fill=blue!20, inner xsep=1pt, inner ysep=1pt},
  background]{} & \ctrl{1}\gategroup[2,steps=1,style={dashed,
    rounded corners,fill=blue!20, inner xsep=1pt, inner ysep=1pt},
  background]{} & \qw & \qw & \ctrl{1}\gategroup[2,steps=1,style={dashed,
    rounded corners,fill=blue!20, inner xsep=1pt, inner ysep=1pt},
  background]{} & \qw
  \\
  \qw & \swap{1} & \qw &
  \targ{}\gategroup[2,steps=2,style={dashed,
    rounded corners,fill=blue!20, inner xsep=1pt, inner ysep=1pt},
  background]{} & \ctrl{1} & \qw & \targ{} & \ctrl{1}\gategroup[2,steps=2,style={dashed,
    rounded corners,fill=blue!20, inner xsep=1pt, inner ysep=1pt},
  background]{} & \targ{} & \targ{} & \qw
  \\
  \qw & \targX{} & \qw &
  \ctrl{-1} & \gate{V} & \gate{V} & \qw & \gate{V^\dagger} & \ctrl{-1} & \qw & \qw
\end{quantikz}

\end{document}
\midstick[3,brackets=none]{=}\qw